# Coding Guidelines

Keep dependencies minimal, prefer standard-library features, preserve Termux compatibility, and avoid hidden network calls. Use small modules with explicit responsibilities. Add tests for tokenizer behavior, model serialization, CLI configuration precedence, and new integrations.

Comments should explain non-obvious numerical logic or platform-specific behavior rather than restating obvious code.
