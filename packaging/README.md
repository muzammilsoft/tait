# Packaging

The application has one canonical Python implementation under `src/tait`.

`packaging/termux/` and `packaging/debian/` are integration templates for future package repositories. They are not themselves the published `pkg` or `apt` repositories.
